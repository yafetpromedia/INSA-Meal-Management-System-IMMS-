import { ModuleStub } from '@/components/ModuleStub';

export default function CampusesPage() {
  return (
    <ModuleStub
      title="Campuses"
      description="Manage universities and training centers across Ethiopia."
      endpoint="/campuses"
    />
  );
}
